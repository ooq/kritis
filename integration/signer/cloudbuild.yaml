steps:
- name: 'gcr.io/cloud-builders/go'
  args: ['build', './cmd/kritis/signer']
  env: ['PROJECT_ROOT=github.com/grafeas/kritis']
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: /bin/bash
  args:
    - -c
    - |
      pwd & ls -al && /workspace/signer --help && cd integration/signer && ./signer_int.sh